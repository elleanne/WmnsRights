package wmnsRights.WmnsRights;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class WmnsRightsApplication {

	// Main class that runs the app

	// use  ./mvnw spring-boot:run if in macOS
	// use  mvnw spring-boot:run if in windowsOS
	public static void main(String[] args) {
		SpringApplication.run(WmnsRightsApplication.class, args);
	}
}
